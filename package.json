{
  "name": "nodejs_ts_template",
  "version": "1.0.0",
  "description": "Minimal TS Template",
  "main": "build/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "dev": "nodemon src/index.ts",
    "start:dev": "npx nodemon",
    "start": "npm run build && node build/index.js"
  },
  "devDependencies": {
    "@types/node": "^18.11.11",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "axios": "^1.2.1"
  }
}
